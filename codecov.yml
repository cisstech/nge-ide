# The v22 UI kit and theme are largely presentational, so adding them lowers the
# overall ratio without a real regression. Track project coverage but don't block
# on it; the patch (new/changed lines) target still applies.
coverage:
  status:
    project:
      default:
        informational: true
